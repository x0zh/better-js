// 版本信息
const VERSION = '1.0.0';

// 默认语言
const LANG = 'zh_CN';

// 默认的时期格式
const DATE_FORMAT = 'yyyy-MM-dd HH:mm:ss';

// 默认中文的星期格式
const ZH_WEEK = ['日', '一', '二', '三', '四', '五', '六'];

// 默认英文日期格式
const EN_WEEK = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'];

// 默认中文上午/下午
const ZH_MERIDIEM = ['上午', '下午'];

// 默认英文上午/下午
const EN_MERIDIEM = ['AM', 'PM'];

export {
    VERSION,
    LANG,
    DATE_FORMAT,
    ZH_WEEK,
    EN_WEEK,
    ZH_MERIDIEM,
    EN_MERIDIEM
}